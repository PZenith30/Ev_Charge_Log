-- =====================================================================
-- Migration 002 — เพิ่มรูปรถ
--
-- สำหรับคนที่รัน schema.sql ไปแล้วก่อนหน้านี้ ไม่ต้องรันไฟล์ใหญ่ซ้ำ
-- เปิด Supabase Dashboard > SQL Editor > New query > วางไฟล์นี้ > Run
-- (ถ้ารัน supabase/schema.sql เวอร์ชันล่าสุดไปแล้ว ไฟล์นี้ไม่ต้องรัน — รันซ้ำก็ไม่พัง)
--
-- เก็บเป็น path ของไฟล์ในบัคเก็ต charge-images ที่มีอยู่แล้ว
-- ไม่ต้องสร้างบัคเก็ตใหม่และใช้ policy เดิมได้เลย เพราะ path ขึ้นต้นด้วย <user_id>/
-- =====================================================================

alter table public.cars add column if not exists photo text;
