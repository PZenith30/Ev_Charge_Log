-- เพิ่มช่อง "ส่วนลด" ให้ตารางการชาร์จ
--
-- รันไฟล์นี้ใน Supabase SQL Editor ถ้าเคยรัน schema.sql ไปแล้วก่อนหน้านี้
-- (ถ้ายังไม่เคยรัน ให้รัน supabase/schema.sql อย่างเดียวก็พอ มีคอลัมน์นี้อยู่แล้ว)
--
-- รันซ้ำได้ไม่พัง เพราะใช้ if not exists
--
-- หมายเหตุ: คอลัมน์ fee ยังชื่อเดิม แม้หน้าจอจะเปลี่ยนไปเรียกว่า "ค่าปรับ" แล้ว
-- เปลี่ยนชื่อคอลัมน์จะทำให้ข้อมูลเดิมของผู้ใช้ที่ยังไม่ได้อัปเดตอ่านไม่ได้

alter table public.charge_sessions add column if not exists discount numeric;
