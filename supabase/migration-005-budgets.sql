-- เพิ่มงบประมาณแยกตามชนิดค่าใช้จ่าย (ค่าชาร์จ · อุปกรณ์เสริม · ประกัน · บำรุงรักษา · ภาษี)
--
-- รันไฟล์นี้ใน Supabase SQL Editor ถ้าเคยรัน schema.sql ไปแล้วก่อนหน้านี้
-- (ถ้ายังไม่เคยรัน ให้รัน supabase/schema.sql อย่างเดียวก็พอ มีคอลัมน์นี้อยู่แล้ว)
--
-- รันซ้ำได้ไม่พัง เพราะใช้ if not exists
--
-- ทำไมเป็น jsonb คอลัมน์เดียว ไม่ใช่คอลัมน์ตัวเลขห้าตัว
--   เพิ่มชนิดใหม่ทีหลังจะได้ไม่ต้องรัน SQL อีกรอบ ซึ่งแปลว่าผู้ใช้ที่ไม่ได้รัน
--   จะไม่เจออาการบันทึกการตั้งค่าไม่ได้ทั้งหมดซ้ำอีก
--
-- คอลัมน์ budget เดิม (งบรวมต่อเดือน) ยังอยู่และยังใช้งานได้เหมือนเดิม
-- ค่าที่เคยตั้งไว้จึงไม่หาย

alter table public.settings
  add column if not exists budgets jsonb not null default '{}'::jsonb;
